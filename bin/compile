#!/bin/bash

indent() {
  sed -u 's/^/       /'
}

echo "-----> Install LibWebP"

BUILD_DIR=$1
CACHE_DIR=$2
VENDOR_DIR="$BUILD_DIR/vendor"
LIBWEBP_FILE="libwebp/libvpx.tar"
LIBWEBP_DIR="libwebp"

apt-get install php7.0-gd