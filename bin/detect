#!/bin/sh

# this pack is valid for apps with a hello.txt in the root
if [ -f $1/libwebp/libwebp.tar ]; then
  echo "libwebp detected, starting compiling..."
  exit 0
else
  echo "libwebp not found. please add the source code at libwebp/libwebp.tar inside your project..."
  exit 1
fi